{
  "scripts": {
    "build": "pug src/pug/ -o dist/ -P && tsc --outDir dist --rootDir src/ts --target ES2020 --module ES2015 --strict && tailwindcss -i src/styles/main.css -o dist/main.css --minify && cp -r assets/* dist/ 2>/dev/null || true"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "prettier": "^3.8.3",
    "pug-cli": "^1.0.0-alpha6",
    "tailwindcss": "^4.3.0",
    "typescript": "^6.0.3"
  }
}
